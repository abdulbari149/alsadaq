/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { remotePatterns: [{ hostname: 'localhost' }, { hostname: 'fspsgbvhllrfzmrvaptl.supabase.co' }] },
};

export default nextConfig;
