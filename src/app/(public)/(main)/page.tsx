import HomePage from "@/components/screens/Home";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Home | Al Sadaq'
}

const page = () => <HomePage />

export default page;