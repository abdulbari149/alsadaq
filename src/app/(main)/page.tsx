import HomePage from "@/components/screens/home";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Al Sadaq | Home'
}

const page = () => <HomePage />

export default page;